# ── AFTER THIS DEPLOY (timezone fix) ─────────────────────────────────────────
#  The server now PINS the device clock to the Device Timezone setting (the
#  device menu is locked under ADMS, so the server sets it). No manual device
#  step needed.
#  1. Portal → HR → Settings → Device Timezone = Asia/Karachi (PKT),
#     Display Timezone = PKT.
#  2. Within ~1 minute (next device poll) the device clock moves to PKT and
#     stays there.
#  3. Verify: device clock shows PKT and no longer drifts; a fresh punch shows
#     the correct PKT time on the right day.
# ─────────────────────────────────────────────────────────────────────────────
cd /home/funnelatics-hris/htdocs/hris.funnelatics.com
git fetch origin && git reset --hard origin/main
source venv/bin/activate
pip install -r requirements.txt
python manage.py migrate
python manage.py collectstatic --noinput
deactivate
chown -R funnelatics-hris:funnelatics-hris /home/funnelatics-hris/htdocs/hris.funnelatics.com
systemctl restart funnelatics-hris.service
