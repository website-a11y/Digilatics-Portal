# ── BEFORE/AFTER THIS DEPLOY (timezone fix) ─────────────────────────────────
#  The server no longer sets the device clock (no TimeZone in the handshake).
#  1. On the DEVICE itself, set its clock/timezone to Pakistan (PKT, UTC+5).
#  2. After deploy: Portal → HR → Settings → Device Timezone = Asia/Karachi
#     (must match the device's actual clock) and Display Timezone = PKT.
#  3. Verify: the device clock no longer reverts after a poll, and a fresh
#     punch shows the correct PKT time on the right day.
# ─────────────────────────────────────────────────────────────────────────────
cd /home/funnelatics-hris/htdocs/hris.funnelatics.com
git fetch origin && git reset --hard origin/main
source venv/bin/activate
pip install -r requirements.txt
python manage.py migrate
python manage.py collectstatic --noinput
deactivate
chown -R funnelatics-hris:funnelatics-hris /home/funnelatics-hris/htdocs/hris.funnelatics.com
systemctl restart funnelatics-hris.service
